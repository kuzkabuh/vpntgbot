<!--
Версия файла: 1.1.0  
Описание: README для проекта VPN-платформы (WireGuard + WG-Easy + FastAPI + Telegram-бот)  
Дата изменения: 2025-12-29
-->

# 🚀 KUZKA VPN Platform (vpntgbot) — v1.2.0

**KUZKA VPN Platform** — это современная VPN-платформа на базе **WireGuard**, включающая:
- панель управления **WG-Easy**;
- backend-сервис на **FastAPI**;
- Telegram-бот для взаимодействия с пользователями.

**Цель платформы** — безопасная и удобная выдача VPN-конфигураций пользователям через Telegram и web-интерфейс, а также архитектурная готовность к подключению тарификации, биллинга и масштабирования.

---

## 📦 1. Архитектура проекта

Платформа работает в Docker-контейнерах и включает сервисы:

### 🗄 PostgreSQL (`vpn_db`)
Хранилище данных:
- пользователи;
- VPN-аккаунты;
- локации;
- тарифы и др.

---

### ⚙ Backend (`vpn_backend`)
FastAPI-сервис:
- инициализация и миграции БД;
- REST API;
- health-эндпоинт `/health`;
- интеграция с **WG-Easy API**;
- логирование и диагностика.

---

### 🤖 Telegram-бот (`vpn_bot`)
Функциональность:
- авторизация пользователей;
- выдача WireGuard-конфигураций;
- управление подключениями;
- в дальнейшем — подписки и биллинг.

---

### 🌐 Nginx (`vpn_nginx`)
Реверс-прокси:
- проброс HTTP к backend;
- публикация `/health`;
- готов к расширению до HTTPS.

---

### 🛡 WG-Easy (`wg_dashboard`)
Web-панель управления WireGuard:
- доступ на `51821/tcp`;
- VPN-интерфейс `51820/udp`;
- управление peers.

Все сервисы описаны в `docker-compose.yml`  
Общая сеть: **vpn-service_vpn_net**

---

## 🧩 2. Требования

| Компонент | Требование |
|---------|-----------|
| ОС сервера | Ubuntu 22.04 / 24.04 |
| Пакеты | docker, docker compose |
| Порты | 80/tcp · 51820/udp · 51821/tcp |

---

## 📁 3. Структура проекта

```
/opt/vpn-service
├── backend/              # FastAPI backend
├── bot/                  # Telegram-бот
├── deploy/               # Скрипты и инструменты
├── nginx/
│   └── conf.d/           # Конфиги Nginx
├── db-data/              # Данные PostgreSQL
├── backups/              # Бэкапы (будущее)
├── docker-compose.yml
├── .env.example
├── .env
├── .gitignore
└── README.md
```

---

## ⚙ 4. Конфигурация окружения (.env)

Создать `.env`:
```
cp .env.example .env
nano .env
```

### База данных
```
DB_NAME=
DB_USER=
DB_PASSWORD=
BACKEND_DB_DSN=
```

### Backend
```
APP_ENV=
APP_DEBUG=
APP_HOST=
APP_PORT=
BACKEND_BASE_URL=
MGMT_API_TOKEN=
```

### Telegram-бот
```
TELEGRAM_BOT_TOKEN=
```

### WireGuard / WG-Easy
```
WG_HOST=
WG_DEFAULT_LOCATION_CODE=
WG_DEFAULT_LOCATION_NAME=
WG_DASHBOARD_PASSWORD_HASH=
WG_EASY_PASSWORD=
```

### Бэкапы (будущее)
```
BACKUP_ENABLED=
BACKUP_LOCAL_DIR=
BACKUP_RETENTION_DAYS=
BACKUP_REMOTE_HOST=
BACKUP_REMOTE_USER=
BACKUP_REMOTE_DIR=
BACKUP_SSH_KEY=
```

> `.env` **не коммитится**, добавлен в `.gitignore`

---

## 🚀 5. Развёртывание

### 5.1 Клонировать репозиторий
```
cd /opt
sudo git clone git@github.com:kuzkabuh/vpntgbot.git vpn-service
cd vpn-service
```

---

### 5.2 Подготовить `.env`
```
cp .env.example .env
nano .env
```
Заполнить: пароли, токены, DSN, `WG_HOST`, `WG_DASHBOARD_PASSWORD_HASH`, `WG_EASY_PASSWORD`.

---

### 5.3 Запуск
```
sudo docker compose up -d --build
```

---

### 5.4 Проверить контейнеры
```
sudo docker ps
```

Должны быть:
- vpn_db  
- vpn_backend  
- vpn_bot  
- vpn_nginx  
- wg_dashboard

---

### 5.5 Проверка health backend
```
curl http://127.0.0.1/health
```

Пример ответа:
```json
{
  "status": "ok",
  "timestamp": "...",
  "database_ok": true,
  "wg_easy_url": "http://wg_dashboard:51821"
}
```

---

### 5.6 Доступ к WG-Easy
```
http://<SERVER_IP>:51821/
```

Пароль = тот, что соответствует `WG_DASHBOARD_PASSWORD_HASH`.

---

## 🔄 6. Обновление проекта

### 6.1 Обновление кода
```
cd /opt/vpn-service
git pull origin main
sudo docker compose up -d --build
```

### 6.2 Обновление WG-Easy
```
sudo docker compose pull wg_dashboard
sudo docker compose up -d wg_dashboard
```

---

## 🧮 7. Версионирование
Используется **Semantic Versioning**:
```
MAJOR.MINOR.PATCH
```

Текущая версия: **v1.2.0**

Создание релиза:
```
git tag -a v1.2.0 -m "Release 1.2.0"
git push origin v1.2.0
```

---

## 📌 8. План развития
- тарификация и биллинг;
- web-личный кабинет;
- аналитика использования;
- мульти-локации;
- автобакапы;
- усиление безопасности (JWT, RBAC, IP-ограничения).

---

## 📜 9. Лицензия
Будет определена позже. На текущий момент — закрытый продукт.
